# carimbai — overrides pessoais (NÃO commitado)

Este arquivo é seu — fica fora do git (ver `.gitignore`). Use para anotações e
overrides que valem só na sua máquina. Nada aqui deve ser necessário para outra
pessoa rodar o projeto.

<!--
Exemplos do que costuma ir aqui:

## Ambiente local
- Porta do Postgres na minha máquina: 5434 (docker) / 5433 (instalação local)
- Uso o profile `local` em vez de `dev` quando faço X.

## Segredos de dev (meus)
- Coloque valores reais APENAS em variáveis de ambiente ou em
  `.claude/settings.local.json` (env). Não cole segredos de produção aqui.

## Lembretes
- Rodar `docker-compose up -d` antes de subir o app.
-->
