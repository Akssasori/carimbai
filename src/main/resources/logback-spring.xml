<?xml version="1.0" encoding="UTF-8"?>
<!--
  FIX-10 / SEC-026/027 — logging com retenção e canal de auditoria separado.

  • root: console em todos os profiles (jornal do systemd captura em prod).
  • prod: arquivos rotativos diários para application e audit, com retenção.
    Path padrão /var/log/carimbai (override via LOG_DIR).
  • audit: logger dedicado, additivity=false → não duplica no stdout. Use em
    SecurityAudit (AuditService) com nível INFO.
-->
<configuration>

    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    <property name="AUDIT_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %msg%n"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <springProfile name="prod">
        <property name="LOG_DIR" value="${LOG_DIR:-/var/log/carimbai}"/>

        <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/application.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/application-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>50MB</maxFileSize>
                <maxHistory>14</maxHistory>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
            </encoder>
        </appender>

        <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/audit.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/audit-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>50MB</maxFileSize>
                <!-- LGPD: trilha de auditoria 180 dias (~6 meses); ajuste conforme política -->
                <maxHistory>180</maxHistory>
                <totalSizeCap>5GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${AUDIT_PATTERN}</pattern>
            </encoder>
        </appender>

        <logger name="audit" level="INFO" additivity="false">
            <appender-ref ref="AUDIT_FILE"/>
        </logger>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="APP_FILE"/>
        </root>
    </springProfile>

    <springProfile name="!prod">
        <!-- Em dev/local: audit também sai no console pra facilitar inspeção. -->
        <logger name="audit" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
